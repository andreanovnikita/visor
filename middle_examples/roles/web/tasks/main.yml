---
- name: Web role tasks
  hosts: all
  tasks:
    - name: Install packages with dist upgrade (bad)
      apt:
        name: nginx
        state: present
        upgrade: no

    - name: Use latest image tag (bad)
      shell: |
        echo "image: webapp:latest"

    - name: Ignore errors example (bad)
      command: /bin/false
      ignore_errors: false
